// @bible/web — React + Vite app. The thin rendering layer: a Zustand bridge dispatches
// engine Commands and renders GameState + animates GameEvents. Real screens land in Phase 6/7.
export const WEB_PLACEHOLDER = true
