// @bible/engine — the pure, deterministic, serializable game engine.
// ZERO React/DOM/storage imports (CI- and ESLint-enforced). The UI dispatches Commands
// and renders state + events; it never mutates state directly.
export const ENGINE_VERSION = '0.0.0'
